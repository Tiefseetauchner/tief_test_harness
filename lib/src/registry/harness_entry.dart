class HarnessEntry {
  final String name;
  final String functionName;
  final String importPath;

  const HarnessEntry({
    required this.name,
    required this.functionName,
    required this.importPath,
  });
}
